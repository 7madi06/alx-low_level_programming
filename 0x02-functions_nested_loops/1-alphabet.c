#include "main.h"

/**
 * main -Entry point
 * Description: 'prints the alphabet, in lowercase, followed by a new line.'
 * Return: Always 0 (Success)
*/

int main(void)
{
    print_alphabet();

	return (0);
}
